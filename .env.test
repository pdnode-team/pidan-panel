SESSION_DRIVER=memory
DB_DATABASE=tmp/db_test.sqlite3
# Empty value overrides any PIDAN_DATA_DIR from .env so tests always use the
# default <app>/data root with plain Windows/local paths.
PIDAN_DATA_DIR=
